the dashboard and ui are in a good enough spot for now. I want to start on phase 4 of roadmap. lets start with the UI additions and then work on the logic and openai api integration after. I want a new menu item with a camera icon. this will direct to a new page /generate. this page should be similar to others in terms of design, mobile and web friendly. it should have "select customer" or a default "new customer" button if the customer isnt in the DB yet, where the image will eventually be associated with the customer's property on the DB or however the best approach to holding the images will work. then we need to have a place where we can upload or take image. we need a list of sliders and modifiers that can be turned on or off like "gutter lights" "all roof edges" "windows". these modifiers should be under a larger modifier which will be light types. So it would be C9 (ON) -> "gutters" (ON) -> "windows" (off) -> ect. then Mini Lights (ON) -> "gutters" (off by default) -> "bushes" (on). Also an Additions tab where i can turn sliders on for things like Wreath on Door or Garland on windows. ect. This will all be used to feed to the openai image generation for turning the photo of the property into how it will eventually look with the lights and decorations that we offer
